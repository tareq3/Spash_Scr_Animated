<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.silicon.tareq.spash_scr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <!--Launcher Acitivities-->
        <activity android:name=".Launcher_Splash.LauncherActivity" android:theme="@style/Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>  <activity android:name=".Launcher_Splash.Launcher_Extender" android:theme="@style/Launcher"/>

        <activity android:name=".Launcher_Splash.SplashActivity" />



        <activity android:name=".Launcher_Splash.IntoActivity.IntroActivity" android:label="@string/app_name" />

        <activity android:name=".MainActivity"/>
    </application>

</manifest>